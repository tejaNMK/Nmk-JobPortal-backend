"""Master data models package.

Holds reference/lookup tables used to power dropdowns across the app:
countries, locations (dependent on country), notice periods, salary
expectation bands, and target roles (with candidate <-> target role
many-to-many linking for the multi-select autocomplete field).
"""
